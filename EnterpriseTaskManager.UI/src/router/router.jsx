/* import { createBrowserRouter } from 'react-router';
import AppLayout from '../layout/AppLayout';
import App from '../App';
import ProjectTest from '../features/projects/ProjectsTest';

export const router = createBrowserRouter([
  {
    element: <AppLayout />,
    children: [
      {
        path: '/',
        element: <App />,
      },
      {
        path: '/test-projects',
        element: <ProjectTest />,
      },
    ],
  },
]); */
